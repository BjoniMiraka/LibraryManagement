@{
    ViewData["Title"] = "My Dashboard";
}

<div class="container mt-4">
    <h1 class="mb-4">My Dashboard</h1>

    <!-- Statistics Cards -->
    <div class="row mb-4">
        <div class="col-md-4">
            <div class="card text-white bg-primary mb-3">
                <div class="card-body">
                    <h5 class="card-title">Active Loans</h5>
                    <h2>@ViewBag.ActiveLoans</h2>
                    <a asp-action="MyLoans" class="btn btn-light btn-sm mt-2">View All</a>
                </div>
            </div>
        </div>
        <div class="col-md-4">
            <div class="card text-white bg-danger mb-3">
                <div class="card-body">
                    <h5 class="card-title">Overdue Books</h5>
                    <h2>@ViewBag.OverdueLoans</h2>
                    @if (ViewBag.OverdueLoans > 0)
                    {
                        <small>Please return these books as soon as possible!</small>
                    }
                </div>
            </div>
        </div>
        <div class="col-md-4">
            <div class="card text-white bg-info mb-3">
                <div class="card-body">
                    <h5 class="card-title">Active Reservations</h5>
                    <h2>@ViewBag.ActiveReservations</h2>
                    <a asp-action="MyReservations" class="btn btn-light btn-sm mt-2">View All</a>
                </div>
            </div>
        </div>
    </div>

    <!-- Quick Actions -->
    <div class="card mb-4">
        <div class="card-header">
            <h4 class="mb-0">Quick Actions</h4>
        </div>
        <div class="card-body">
            <div class="row">
                <div class="col-md-3">
                    <a asp-controller="Books" asp-action="Index" class="btn btn-outline-primary w-100 mb-2">
                        <i class="bi bi-book"></i> Browse Books
                    </a>
                </div>
                <div class="col-md-3">
                    <a asp-action="MyLoans" class="btn btn-outline-success w-100 mb-2">
                        <i class="bi bi-journal-check"></i> My Loans
                    </a>
                </div>
                <div class="col-md-3">
                    <a asp-action="MyReservations" class="btn btn-outline-info w-100 mb-2">
                        <i class="bi bi-bookmark"></i> My Reservations
                    </a>
                </div>
                <div class="col-md-3">
                    <a asp-action="Profile" class="btn btn-outline-secondary w-100 mb-2">
                        <i class="bi bi-person"></i> My Profile
                    </a>
                </div>
            </div>
        </div>
    </div>

    <!-- Welcome Message -->
    <div class="card">
        <div class="card-body">
            <h5>Welcome to the Library Management System!</h5>
            <p>Here you can:</p>
            <ul>
                <li>Browse and search for books</li>
                <li>Borrow books (14-day loan period)</li>
                <li>Reserve books that are currently unavailable</li>
                <li>Track your loans and due dates</li>
                <li>Manage your reservations</li>
            </ul>
            <p class="mb-0"><strong>Note:</strong> Late returns incur a fee of $1 per day.</p>
        </div>
    </div>
</div>
